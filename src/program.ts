import {Manager} from './managers/manager';

/**
 * Gestor del sistem, básicamente el inicio del programa
 */
export const manager = new Manager();

// Ejecutar el comando npm run program para ejecutar el programa
